name: test build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    #- name: configure
    #  run: ./configure
    - name: cmake test_build
      working-directory: ./tst
      run: cmake ..
    #- name: make check
    #  run: make check
    #- name: make distcheck
    #  run: make distcheck
